# 03 — Triggers & Environment Variables

ไฟล์ `env-and-triggers.yml` แสดง:

- `push` / `pull_request` / `workflow_dispatch`
- `env` ระดับ workflow, job, step
- การอ่าน GitHub contexts

ลองกด **Actions → เลือก workflow → Run workflow** เพื่อทดสอบ `workflow_dispatch`
