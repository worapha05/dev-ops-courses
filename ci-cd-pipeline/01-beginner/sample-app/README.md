# Sample app สำหรับ CI/CD Bootcamp

แอป Node.js เล็ก ๆ ที่ใช้ร่วมทุกระดับ:

- `npm test` — unit tests (node:test)
- `npm run lint` — lint จำลอง
- `Dockerfile` — multi-stage image สำหรับ Expert labs

```bash
npm install
npm test
npm start
# http://localhost:3000/health
```
