apiVersion: v1
kind: Service
metadata:
  name: parcelgo-api
  labels:
    app: parcelgo-api
spec:
  type: ClusterIP
  selector:
    app: parcelgo-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
