apiVersion: v1
kind: Service
metadata:
  name: demo-api-nodeport
  labels:
    app: demo-api
spec:
  type: NodePort
  selector:
    app: demo-api
  ports:
    - name: http
      port: 80
      targetPort: 5678
      nodePort: 30080
