apiVersion: v1
kind: Service
metadata:
  name: demo-api-lb
  labels:
    app: demo-api
spec:
  type: LoadBalancer
  selector:
    app: demo-api
  ports:
    - name: http
      port: 80
      targetPort: 5678
